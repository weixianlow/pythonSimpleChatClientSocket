def readUserFile(userList, passList):
	with open ('User.txt', 'r') as infile:
		for line in infile:
			userList.append(line.rstrip(		passList.append(line.rstrip())
def checkUserExist(list, UserName):
	for i in range(0,6,2):
		if UserName in list:
			return 1;


userList=[]
passList=[]
for line in userList:
	print line

for line in passList:
	print line

readUserFile(userList, passList)


for line in userList:
	print line

print 'done'

for line in passList:
	print line
